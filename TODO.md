# TODO - Cobertura map

- [ ] Confirmar cómo mostrar el mapa en la página de Cobertura (ya existe `CoverageMap` y se usa dentro de `CoberturaHero`).
- [ ] Si el mapa no se ve/está cortado, ajustar layout/estilos: revisar clases `mapa-grande` y el contenedor `mapa-cobertura-hero`.
- [ ] Verificar si `CoverageMap` necesita `style`/`height` adicional (Leaflet a veces requiere altura explícita).
- [ ] Ejecutar `npm run dev` y verificar visualmente en `/cobertura`.
- [ ] Si hay errores en consola, corregir imports/estilos (p.ej. `leaflet.css`, icono, etc.).
